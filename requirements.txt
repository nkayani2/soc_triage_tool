# SOC Alert Triage & Enrichment Tool — Python dependencies
#
# Install with:  pip install -r requirements.txt
#
# Python 3.10+ is required.

# HTTP / API
requests>=2.31.0

# Data handling
pandas>=2.0.0

# Modern ttk widgets + dark theme
ttkbootstrap>=1.10.1

# PDF report generation (optional but enabled by default)
reportlab>=4.0.0

# NOTE: tkinter ships with the standard CPython installer on Windows
# and macOS. On Linux you may need to install it via your package
# manager, e.g.:
#     sudo apt-get install python3-tk
